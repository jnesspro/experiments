ext {
    springBootVersion = '2.3.4.RELEASE'
    testcontainersVersion = '1.15.2'
}

allprojects {
    repositories {
        mavenCentral()
        maven { url "https://repo.spring.io/milestone" }
    }
}